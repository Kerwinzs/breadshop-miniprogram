# 面包坊小程序项目状态

> 这是面向 Codex 多线程协作的共享状态文档。每个线程开始工作前先阅读本文件；完成阶段性工作后更新“当前进度”“待办模块”和“Git 记录”。除非用户明确改变需求，`prd.md`、`AGENTS.md` 和本文件共同构成协作上下文。

## 1. 当前状态

- 项目阶段：本地 Mock MVP 已完成并保存为稳定基线。
- 当前开发主线：`main`。
- 当前本地基线提交：`53cd0db chore: 保存本地 Mock MVP 基线`；本文件的后续文档提交会位于该基线之后。
- 稳定版本标签：`v0.1.0-local-mock-mvp`。
- 当前实现：微信小程序本地 Mock，使用本地 Storage 保存购物车、地址和订单。
- 暂未接入：真实后端、登录、支付、配送服务、邮费 API。
- 当前工作区：已检查为干净；新线程开始前应再次执行 `git status` 确认。

## 2. 已完成能力

- 到店自取流程：门店选择、独立购物车、结算、下单和订单状态推进。
- 同城外卖流程：地址选择、独立配送购物车、费用展示、结算、下单和状态推进。
- 快递邮寄流程：配送方式切换、地址校验、邮费展示、结算、下单和状态推进。
- 地址模块：新增、编辑、删除、选择和默认地址处理。
- 订单模块：订单列表、订单详情、取消订单、Mock 状态推进和“再来一单”。
- 已完成验证：三种购买流程、地址模块、订单状态流转、代码级检查和人工边界测试。

## 3. 当前业务约束

- 自取与配送购物车必须完全独立，下单一个场景不得清空另一个场景。
- 配送订单合计为：商品总额 + 保温包装费 + 配送费/邮费；自取不收取并不展示配送费用。
- 当前 Mock 费用：保温包装费 ¥2.00；同城配送费 ¥6.00；快递邮费 ¥12.00。
- 地址缺失、不完整、超范围、配送方式不支持或费用未计算时，不得显示 `¥0.00` 作为未知费用。
- 下单不触发支付，只生成“已下单”订单并清空已提交场景的购物车。
- 只有“已下单”状态允许取消；只有“已完成”订单支持“再来一单”。
- 状态流转：自取为“已下单 → 制作中 → 待自取 → 已完成”；同城外卖为“已下单 → 制作中 → 配送中 → 已完成”；快递邮寄为“已下单 → 制作中 → 待发货 → 运输中 → 已完成”。以上流程均允许在“已下单”阶段取消。
- `v1/` 是原型对照目录，未经用户明确要求不得修改。

## 4. 协作规则

- 开始任务：先读本文件、`prd.md`、`AGENTS.md`，再检查 `git status` 和当前分支。
- 修改业务行为：同步检查 `prd.md` 和本文件是否需要更新。
- 多线程开发：每个线程使用独立分支；不要直接在 `main` 上进行未确认的大范围改动。
- 完成阶段：运行适用的检查，说明修改文件、测试结果、已知限制，并记录 Git 提交或标签。
- 遇到不确定的未跟踪文件：先报告，不擅自加入版本控制。
- 不使用 `git reset --hard`，不删除用户文件，不覆盖稳定标签。

## 5. 原有 TODO 模块：真实配送与邮费接入方案

> 本模块保留原 `TODO.md` 的待办内容，作为项目状态文档中的一个独立模块。

### 待确认

- [ ] **真实配送与邮费接入方案**
  - 当前状态：暂缓，不改变现有本地 Mock 费用和地址流程。
  - 需要决定：
    - 配送与邮费数据来源（自建后端、微信云开发、第三方物流/运费服务等）；
    - 同城外卖配送范围和计费方式（固定、距离、区域或阶梯）；
    - 快递邮费计算方式（地区、重量、商品类型、包邮规则等）；
    - 前端展示方式（实时计算、提交前确认、费用明细和不可配送提示）；
    - 费用计算失败、地址超范围、配送方式不可用时的交互；
    - 是否保存行政区编码、配送区域编码及费用计算快照。
  - 当前约束：在方案确认前，不接入真实配送接口，不把 Mock 金额当作正式业务规则。
  - 关联文件：`prd.md`、`关键交互场景决策文档.md`、`miniprogram/utils/fee-service.js`。

## 6. Git / GitHub 记录

### 本地基线

- 初始稳定基线提交：`2a4b875 chore: 保存本地 Mock MVP 基线`。
- 稳定标签：`v0.1.0-local-mock-mvp`，指向初始基线提交。
- 后续因同步 GitHub 初始化提交执行了 rebase，当前本地等价基线提交为：`53cd0db chore: 保存本地 Mock MVP 基线`，其父提交为远程初始化提交 `dd25aa3`。
- 本地仓库当前分支：`main`；在本次文档修改前工作区干净。

### GitHub 远程

- 仓库：[Kerwinzs/breadshop-miniprogram](https://github.com/Kerwinzs/breadshop-miniprogram)。
- 可见性：Private。
- 远程 `main` 当前为 GitHub 初始化提交 `dd25aa3 Initial commit`；本地基线尚未完成推送。
- 远程标签 `v0.1.0-local-mock-mvp` 已存在，并指向初始基线提交 `2a4b875`。
- 后续同步本地主线前，应先处理本地 `53cd0db` 与远程 `dd25aa3` 的关系；不要强制覆盖远程历史。

### 本次状态文档变更

- 本次变更：将原 `TODO.md` 扩展为共享项目状态文档，并完整保留原有真实配送与邮费待办模块。
- 本次变更还记录了当前基线、GitHub 远程状态、分支和标签信息。
- 完成后应提交本文件；如远程推送网络正常，再推送本地 `main`。

## 7. 下一步建议

1. 先确定后端技术方案和部署边界，暂不接入支付。
2. 设计商品、门店、地址、购物车和订单数据模型及 API 契约。
3. 创建独立后端开发分支，保留本地 Mock 作为可切换的开发降级方案。
4. 后端基础能力验证通过后，再逐步替换小程序 Storage 读写。
