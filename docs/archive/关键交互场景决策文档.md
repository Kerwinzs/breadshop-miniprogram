# 面包坊小程序关键交互场景决策文档

> 状态：开发前待客户确认
>
> 用途：冻结会影响数据模型、费用计算、页面状态和验收路径的规则。未确认的项目不得在代码中写死。

## 1. 当前 V1 已覆盖与未覆盖

V1 已有主要理想路径：

- 首页与“到店自取”“外卖/邮寄”双入口
- 自取商品列表、配送商品列表、商品详情
- 自取购物车、配送购物车
- 自取结算、配送结算、下单成功
- 订单列表、两类订单详情
- 地址管理、门店选择、个人中心
- 基础空购物车

V1 目前仍需要补充或确认：

- 页面加载态、网络失败态和提交失败态
- 无订单、无地址、无可用门店、门店打烊等空/异常状态
- 售罄、配送范围外、邮费无法计算的阻断反馈
- 双购物车切换、首页购物车展示规则
- Mock 订单状态推进工具
- 包装费、配送费、邮费和门槛的正式计算规则

## 2. 场景切换与双购物车

### 2.1 购物车隔离

维护两个独立数组：

```js
pickupCartItems: []
deliveryCartItems: []
```

同城外卖与快递邮寄共用 `deliveryCartItems`，切换配送方式只重新计算配送可用性和费用，不清空商品。

### 2.2 从一个场景切换到另一个场景

建议规则：

- 不弹阻断式确认框，允许直接切换。
- 进入目标场景后，只展示目标场景的购物车。
- 如果另一场景有商品，在场景切换入口附近展示非阻断提示，例如“自取购物车还有 2 件商品”。
- 清空购物车、下单和结算时必须明确标明作用范围。

原因：两套购物车本来就是独立的，切换不应被误认为会清空商品；但用户需要知道另一套购物车仍然存在。

### 2.3 浮动购物车栏

- 首页未选择场景时，不显示单一金额，避免金额归属不清；显示“购物车”并展示自取/配送两个数量徽标。
- 商品列表和详情页只显示当前场景购物车的数量和商品小计。
- 购物车页面使用场景 Tab 显示两套购物车，不把两套商品合并计算。

### 2.4 再来一单

建议规则：

- 将原订单中仍可购买的商品追加到原订单对应场景的购物车。
- 不清空购物车中已有商品。
- 数量按“原数量 + 当前已有数量”叠加。
- 商品售罄、规格下架或配送方式不可用时，保留可购买项，并列出未加入项及原因。
- 加购成功后进入对应购物车，并提示“已加入自取购物车”或“已加入配送购物车”。

## 3. 加载、空状态、错误和边界状态

每个异步页面至少定义以下状态：

| 状态 | 示例页面 | 用户反馈 |
|---|---|---|
| loading | 首页、商品列表、订单列表 | 骨架屏或加载提示 |
| empty | 空购物车、无订单、无地址、无门店 | 说明原因和下一步按钮 |
| unavailable | 售罄、门店打烊、不可配送 | 明确原因，禁止继续操作 |
| error | 商品加载失败、费用计算失败、提交失败 | 重试按钮和保留当前输入 |
| submitting | 确认下单 | 按钮进入提交中，防止重复提交 |
| success | 下单成功、地址保存成功 | 明确成功反馈和下一步入口 |

门店打烊时允许浏览，但应禁止选择当前时段的自取并说明营业时间。售罄商品不可加购；购物车中已售罄商品进入结算时必须拦截并允许移除。

## 4. Mock 订单状态推进

本地 Mock 阶段增加仅开发环境可见的“模拟商家处理”工具：

- 订单详情显示“推进到下一状态”按钮，或在开发调试面板集中推进。
- 每次只能推进到当前订单类型的下一个合法状态。
- 取消只允许在“已下单”状态执行。
- 自取：已下单 → 制作中 → 待自取 → 已完成。
- 同城外卖：已下单 → 制作中 → 配送中 → 已完成。
- 快递邮寄：已下单 → 制作中 → 待发货 → 运输中 → 已完成。
- 生产构建必须移除该按钮；Mock 工具不能出现在客户正式体验版本。

这样可以完整演示“订单创建、状态跟踪、完成、再来一单”，同时不伪造真实商家端能力。

## 5. 费用规则待确认项

客户需要明确以下输入，开发不能自行假定：

### 5.1 保温包装费

- 固定按订单收取，还是按商品数量收取？
- 哪些商品必须保温包装？
- 是否存在包装费上限或免包装条件？

### 5.2 同城外卖配送费

- 固定金额，还是按距离/区域/金额阶梯计算？
- 起送价是多少？按商品总额还是订单合计判断？
- 超出配送范围时是否允许切换快递邮寄？

### 5.3 快递邮费

- 固定邮费、按地区、按重量，还是阶梯邮费？
- 包邮门槛是多少？按商品总额判断还是按可计费商品判断？
- 哪些商品不可快递？

### 5.4 MVP 建议

如果客户暂时没有正式计费规则，Mock 版本可以采用配置化固定值，但必须标记为演示规则：

```js
deliveryConfig = {
  insulation: { type: 'perOrder', feeFen: 300 },
  local: { type: 'fixed', feeFen: 800, minSubtotalFen: 3500 },
  shipping: { type: 'fixed', feeFen: 1200, freeAboveFen: 9900 }
}
```

正式费用应替换配置，不应散落在页面代码中。

## 6. 地址与配送方式

地址模型建议增加能力字段：

```js
{
  id: 'address-001',
  contactName: '张女士',
  phone: '13800002211',
  province: '上海市',
  city: '上海市',
  district: '静安区',
  detail: '愚园路 601 号 2 号楼 601 室',
  supportsLocal: true,
  supportsShipping: true,
  isDefault: true
}
```

规则：

- 同一个地址可以同时支持同城外卖和快递邮寄，也可以只支持其中一种。
- 切换到不支持的配送方式时，不删除地址，提示“当前地址不支持同城外卖，请修改地址或选择快递邮寄”。
- 地址为空、字段不完整、超出配送范围、配送费计算失败要区分展示。
- 费用状态建议统一为：`missingAddress`、`incompleteAddress`、`outOfRange`、`feeUnavailable`、`ready`。
- 只有 `ready` 才显示具体配送费/邮费；其他状态显示对应原因，不显示 `¥0.00`。

## 7. 可替换的数据访问层

页面不能直接大量调用 `wx.setStorageSync`。先定义可替换的数据接口，第一版由本地实现，后续替换为云函数或 HTTP API：

```js
productRepository.list()
cartRepository.get(scene)
cartRepository.update(scene, items)
orderRepository.submit(payload)
orderRepository.list(filters)
orderRepository.cancel(orderId)
orderRepository.advanceMock(orderId)
addressRepository.list()
feeService.calculate(input)
```

页面只关心“查询、提交、成功或失败”，不直接关心数据来自本地存储还是服务器。

## 8. 开发前客户确认清单

- [ ] 加载、空状态和错误状态的文案与视觉
- [ ] 场景切换是否采用非阻断提示
- [ ] 首页浮动购物车的展示方式
- [ ] 再来一单是追加，并确认售罄处理方式
- [ ] Mock 状态推进仅开发可见
- [ ] 保温包装费规则
- [ ] 同城外卖配送费和起送价规则
- [ ] 快递邮费和包邮规则
- [ ] 商品是否支持同城外卖/快递邮寄
- [ ] 地址可配送范围和地址校验规则
- [ ] 正式后端方案：云开发或自建后端

在这些项目确认前，只应搭建页面骨架和接口，不应把金额、可配送性和状态流程写死。
