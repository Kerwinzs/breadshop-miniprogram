# 退款状态对账修复（2026-09-12）

## 当前事实

- 曾有多笔已完成微信退款在后台显示“退款处理中”，原因是退款发起后仅写入 `processing`，缺少主动查询补偿。
- `payment-worker` 已增加每分钟查询 `processing` 退款的逻辑；成功或失败会在校验退款单号和金额后同步 `refunds` 与 `orders`。
- 部署后已验证多笔历史退款同步为 `succeeded`，对应订单的 `refundStatus` 和 `refundedAmountFen` 已更新。
- 另有一笔 5 分退款仍待处理，微信返回基本账户余额不足；充值后由定时任务继续重试。

## 未完成

- 待用户充值商户基本账户后，回归确认 5 分退款最终成功。
