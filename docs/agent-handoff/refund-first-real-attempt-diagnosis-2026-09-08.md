# 首次真实退款尝试只读诊断（2026-09-08）

## 1. 完成内容

- 已对 CloudBase 测试环境 `cloud1-d9gc800bmc6952073` 中最近一次真实支付后的退款故障完成只读核验。
- 已核对订单、支付交易、退款队列、退款结果事件、相关函数、定时器及非敏感健康状态。

## 2. 修改文件

- 新增本交接记录。
- 未修改业务代码或云端资源。

## 3. 数据库结构影响

无。仅执行字段白名单查询，未创建、修改或删除任何数据、集合、权限或索引。

## 4. API 变化

无。未发起支付、退款、查单、关单或 worker 重试。

## 5. 模块影响

无线上变更。核验范围仅涉及订单支付状态、退款队列状态及相关运行资源。

## 6. 重要时间线

- `2026-09-07 20:52:05`：订单创建。
- `2026-09-07 20:52:15`：支付成功。
- `2026-09-07 20:53:23`：订单取消并创建退款请求。
- worker 已领取并处理退款两次；退款记录 `attemptCount` 从初始值 1 增至 3。
- 最近一次处理记录为 `2026-09-07 21:02:01`，当时安排下一次尝试时间为 `2026-09-07 21:10:01`。

## 7. 未解决问题

- 订单仍为 `paymentStatus: paid`、`refundStatus: pending`。
- 退款记录仍为 `status: pending`，退款金额尚未回写为已退。
- 未发现退款结果事件，因此没有证据证明微信侧退款已受理或完成。
- 未查询云函数日志，因为现有 MCP 日志能力不能保证排除完整正文及敏感内容。

## 8. 验证方式与结果

- 支付交易存在，状态为 `paid`，支付金额 1 分。
- 退款请求存在，金额 1 分，最近脱敏错误码为 `INVALID_WECHATPAY_SIGNATURE`。
- 退款结果事件数量为 0。
- `payment`、`payment-worker`、`payment-notify` 均为 `Active`。
- `payment` 非敏感健康检查结果为 `configured: true`。
- `merchant-admin` 非敏感健康检查结果为 `refundQueueEnabled: true`。
- `payment-worker-every-minute` 定时器已启用，状态为 `Available`，计划每分钟执行。

## 9. 原因判断与下一步建议

- 已核验事实：worker 已领取退款任务，但微信退款接口响应验签失败，错误码为 `INVALID_WECHATPAY_SIGNATURE`。
- 推断：故障可能位于微信支付平台公钥标识、公钥或证书、响应签名头、原始响应体处理中的一项；现有安全证据不足以进一步区分。
- 建议先修复退款响应验签链路，再经明确授权重试该退款；在故障修复前继续自动重试预计会重复失败。
