# 面包坊小程序开发约束

本文件是本项目的开发代理（Agent）和协作者的统一工作约定。所有开发、设计还原、原型调整和微信小程序转换工作都必须遵守本文件；如本文件与用户最新明确需求冲突，以用户最新需求为准，并同步更新本文件或 `prd.md`。

## 1. 项目定位与当前阶段

- 项目：面包坊小程序，核心能力是商品浏览、场景化购物车、微信支付下单和订单跟踪。
- 产品形态：微信小程序；当前仓库同时保留 HTML/CSS/JavaScript 交互原型，后续再转换为真实小程序页面。
- 交易范围：支付模块进入本地开发阶段，三种购买方式均采用全额微信支付；运输费用继续到付。当前任务不注入真实商户密钥、不部署、不产生真实资金扣款。余额、部分退款仍不在首期范围。
- 当前原型入口：`prototype/`。原型页面和 `prototype/app.js`、`prototype/app.css` 共同构成可交互基线。
- 转换规范：`html-to-wechat-miniprogram/SKILL.md` 及其 `references/conversion-rules.md` 是 HTML 原型转换为微信小程序时的强制规范。

## 2. 需求与事实来源

按以下优先级处理需求：

1. 用户在当前任务中的最新明确要求。
2. `prd.md` 中的产品规则、页面功能和边界条件。
3. 本文件中的工程约束。
4. 现有原型实现和视觉稿。

修改行为、状态、费用、页面路径或交互流程时，先检查 `prd.md` 是否需要同步；不要只修改页面文案而留下旧流程。若需求存在歧义且会影响数据模型、页面结构或用户承诺，应先说明假设再实现。

## 3. 核心业务规则（不可破坏）

### 3.1 购买场景

- 用户必须在“到店自取”和“外卖/邮寄”之间选择场景。
- “到店自取”使用门店信息；“外卖/邮寄”使用收货地址。
- 配送场景还要选择“同城外卖”或“快递邮寄”。配送方式可以在结算前切换，提交订单后不可修改。
- 自取购物车和配送购物车完全独立，增减、清空、结算和下单不得互相影响。
- 页面、购物车、确认订单和订单详情都要持续显示当前场景；配送订单还要显示配送方式。

### 3.2 费用口径

同城外卖与快递邮寄的线上订单合计均只计真实商品小计，运输费用采用到付。

- 同城外卖不另收固定保温包装费或配送费；固定分类“拍前必读”的三件同城真实商品各自动加入 1 件且不可删除或增减，其中同城外卖打包费按商家配置的配送价计入商品小计，同城配送费到付和外卖拍前必读固定为零价。
- 快递邮寄不另收固定保温包装费或邮费；固定分类“拍前必读”的三件快递真实商品各自动加入 1 件且不可删除或增减，其中包装泡沫冰袋按商家配置的配送价计入商品小计，默认顺丰特快到付和拍前必读固定为零价。
- 地址未填写或费用尚未计算时显示“待填写地址后计算”或“暂无法计算”，不能误显示为 `¥0.00`。
- 自取不收取保温包装费、配送费或邮费；费用项是否展示必须与 `prd.md` 保持一致。
- 所有金额统一保留两位小数。实现层建议使用“分”为整数进行计算，展示层再格式化为人民币金额，避免浮点误差。
- 同城外卖和快递邮寄均不设起送价或包邮门槛；配送是否可结算仅取决于地址、配送范围和费用计算结果。

### 3.3 订单状态

- 自取：`已下单 → 制作中 → 待自取 → 已完成 / 已取消`。
- 同城外卖：`已下单 → 制作中 → 配送中 → 已完成 / 已取消`。
- 快递邮寄：`已下单 → 制作中 → 待发货 → 运输中 → 已完成 / 已取消`。
- 履约状态与资金状态必须分离。新订单先进入 `pending`（待支付），支付成功后才允许商家把履约状态从“已下单”推进到“制作中”。
- 待支付有效期为 15 分钟；超时必须关闭支付单并幂等释放库存。
- 用户可在制作开始前取消：未支付订单直接关闭；已支付订单进入整单退款处理中。订单取消、退款申请、退款成功和退款失败必须分别记录，不能用“已取消”代表“已退款”。
- 首期只支持整单全额原路退款，不支持部分退款。退款失败必须保留可重试状态，不得伪装成功。
- 已完成订单支持“再来一单”，并加入原订单对应场景的购物车。

## 4. 目录与文件职责

### 4.1 原型目录

- `prototype/*.html`：页面级原型；新增可独立访问的页面时创建独立 HTML，不要用一个页面状态变量伪装所有页面。
- `prototype/app.js`：原型共享状态、事件和 mock 交互。修改共享状态时必须检查所有页面的数据属性和显示文案。
- `prototype/app.css`、`prototype/atelier.css`：共享视觉样式。优先复用现有设计变量和组件样式，不在单页重复写同一套规则。
- `prototype/*.html.artifact.json`：原型工具生成的伴随元数据。除非任务明确要求，不手工改写或删除。

### 4.2 真实小程序目录

实现微信小程序时使用独立的 `miniprogram/`（或项目已有的小程序根目录）：

```text
miniprogram/
  app.json
  app.js 或 app.ts
  app.wxss
  pages/
    home/
    products/
    product-detail/
    cart/
    checkout/
    orders/
    order-detail/
    profile/
    addresses/
    stores/
  components/
  utils/
  assets/
```

- 每个可访问页面必须有独立目录，并包含对应的 `.wxml`、`.wxss`、`.js/.ts`、`.json`。
- `app.json` 是页面和底部 Tab 导航的唯一声明处；首页、订单、个人为主 Tab。
- 详情、购物车、结算、地址、门店和订单详情等流程页不得放入 `tabBar`。
- 页面标题写入页面 `.json` 的 `navigationBarTitleText`，默认使用微信原生导航栏。
- 主 Tab 必须使用 `app.json` 的原生 `tabBar`，不要在每个页面复制手写底部导航。
- 只有页面独有的提交栏、购买栏或确认栏可以使用固定底部布局，并且必须为原生 TabBar 和安全区预留空间。

## 5. 微信平台实现要求

转换或新建小程序页面前，必须阅读 `html-to-wechat-miniprogram/SKILL.md` 和 `references/conversion-rules.md`。

- 普通页面使用 `wx.navigateTo`；Tab 页面使用 `wx.switchTab`；提交完成替换页面时使用 `wx.redirectTo`。
- 不得使用 `wx.navigateTo` 打开 Tab 页面，也不得给 `wx.switchTab` 传 query 参数；跨 Tab 状态使用小型本地 store 或 `wx.setStorageSync`。
- 不得用单页 `currentPage`、`activeScreen` 或 `routeName` 状态模拟整个应用。
- `tabBar` 图标必须是本地资源，不依赖远程 URL。
- 所有页面和固定底部操作栏都要处理刘海屏、底部手势区和原生 TabBar 遮挡。
- 未接入后端前使用明确的本地 mock 数据；不要假设存在未声明的 API、登录态或支付服务。

## 6. 交互与视觉约定

- 用户可见文案优先使用简洁中文，沿用 PRD 的术语：“到店自取”“外卖/邮寄”“同城外卖”“快递邮寄”“保温包装费”“配送费”“邮费”。不要在新页面恢复“外卖到家”作为主入口名称。
- 费用名称、状态名称和场景标签在所有页面保持完全一致。
- 商品售罄、地址缺失、邮费无法计算和空购物车都必须有明确反馈；不可只通过按钮置灰表达。
- 交互状态要可见：加载、提交中、成功、失败、空状态和不可配送状态都需要对应 UI。
- 优先复用现有原型的色彩、间距、圆角、字体层级和按钮样式；新样式应集中在共享样式或组件中。
- 图片和图标优先使用仓库本地资源；不要在生产代码中依赖临时截图或不可追溯的外链资源。

## 7. 数据与状态约定

建议统一使用以下字段语义（字段名可按最终技术栈调整，但含义不能改变）：

```js
{
  purchaseScene: 'pickup' | 'delivery',
  deliveryMethod: 'local' | 'shipping',
  storeId: string,
  addressId: string,
  cartItems: [],
  deliveryCartItems: [],
  insulationFeeFen: number,
  deliveryFeeFen: number,
  postageFen: number,
  orderStatus: string,
  paymentStatus: 'pending' | 'paid' | 'closed',
  refundStatus: 'none' | 'pending' | 'succeeded' | 'failed',
  payableAmountFen: number,
  paidAmountFen: number,
  refundedAmountFen: number
}
```

- 不要把自取和配送商品混存后再依赖页面筛选来区分。
- 切换配送方式时保留商品列表，但重新计算可配送性、保温包装费和配送费/邮费。
- 下单只清空已提交的对应购物车，不清空另一场景购物车。
- 本地 mock 状态应集中管理，避免在多个页面各自维护一份购物车或订单数据。

## 8. 开发工作流

每次实现需求都按以下顺序执行：

1. 阅读本文件和 `prd.md`，定位受影响的页面、共享状态和费用/订单规则。
2. 先列出页面、数据和交互影响；涉及微信平台转换时先设计 `app.json` 和页面路由。
3. 做最小范围修改，优先复用现有组件、样式和 mock 数据。
4. 检查所有相关入口和返回路径：首页、商品列表、详情、购物车、结算、下单成功、订单列表、订单详情、个人中心。
5. 运行可用的静态检查或本地预览，至少验证控制台无新增错误、页面链接可用、空状态和异常状态可达。
6. 汇报修改文件、验证结果、已知限制和需要用户决定的事项。

如果需求改变了产品行为，先更新 `prd.md` 再改代码，或在同一变更中同步更新。`snapshots/` 下的历史基线只用于对照，不作为当前实现的修改目标。

## 9. 提交前验收清单

- [ ] 首页入口名称、说明和跳转场景正确。
- [ ] 自取与配送购物车数量、金额、清空和结算互不影响。
- [ ] 配送方式切换后，地址、费用和订单标签同步更新。
- [ ] 保温包装费、配送费/邮费在列表提示、购物车、结算和订单详情中显示一致。
- [ ] 地址缺失、配送范围外、邮费无法计算、售罄和空购物车均有可理解的反馈。
- [ ] 新订单先进入待支付；只有服务端确认支付成功后才清空对应购物车并允许进入制作。
- [ ] 支付取消、失败、结果未知、超时关单、重复通知和退款失败均有明确且可重试的状态。
- [ ] 商户私钥、API v3 密钥、证书、签名、完整通知报文和临时支付参数不进入小程序、网页、Git、聊天或普通日志。
- [ ] 取消权限、订单状态流转和“再来一单”符合 PRD。
- [ ] 小程序页面均已声明，Tab 导航使用原生 `tabBar`，跳转 API 使用正确。
- [ ] 没有新增远程资源依赖、假顶部导航、重复底部导航或单页状态机。
- [ ] 未修改历史快照和无关生成文件。

## 10. 变更记录要求

涉及以下内容时，在 PR 或交付说明中简要记录变更原因和影响范围：页面路由、购买场景、费用计算、订单状态、数据字段、共享组件、微信平台行为或本地存储结构。不要用“优化”“调整”等无法复核的描述代替具体变更。

## 11. 当前项目结构真相源

- `prd.md` 是产品规则唯一依据；`brand-spec.md` 是当前品牌视觉唯一依据。
- `prototype/` 是原始功能基线，只读对照；`bp_version/` 是当前设计变体，视觉修改只在此目录进行。
- `bp_version/base.css` 负责基础布局与组件结构；`bp_version/theme.css` 负责品牌颜色、字体和视觉状态。
- 不要继续向 `bp_version/app.css` 追加临时主题层；新视觉修改统一写入 `theme.css`。
- 品牌参考图统一存放在 `references/brand/`；`snapshots/` 只读，不作为当前实现修改目标。

## 12. Agent 协作与共享知识管理

### 12.1 主 Agent 职责

- 主 Agent 负责整体规划、任务拆分、子线程调度、进度管理和最终验收；具体编码、文档或验证工作应按影响范围派给合适的执行线程，主线程保留跨线程协调和收口职责。
- 派单前判断线程经验、业务对象、数据库/API/权限和公共模块影响，明确所需上下文、可修改范围、验证方式及是否允许云端操作。
- 主 Agent 负责跨线程一致性：避免并发修改同一核心文件；检查数据库/API 契约、依赖顺序、汇总结果和最终验证；进入下一阶段前确认上一阶段已有可复核证据。

### 12.2 线程上下文与派单

- 已有线程优先复用已有项目上下文。仅当涉及不确定的业务规则、数据库字段、API 契约、权限规则或设计规范时按需查阅，不因新一轮任务重复全文阅读无关资料。
- 新线程先读取 `AGENTS.md`、相关 `docs/agent-handoff/` handoff、直接相关的需求/数据库/API/设计文档，完成最小上下文后再开始；不得无目的通读全项目。
- 默认派单格式：已有线程写明“继续基于已有上下文，无需重新完整阅读，遇不确定按需查阅”；新线程写明“先读取 AGENTS.md、相关 handoff 和直接相关文档，完成最小上下文后开始”。
- 任务涉及云端数据库、CloudBase、真实微信身份、权限或历史数据时，派单必须明确只读/可写边界、目标环境、禁止操作和证据要求。未明确授权不得删除、覆盖或迁移数据。

### 12.3 子线程回报要求

子线程完成后必须结构化回报：

1. 完成内容；
2. 修改文件；
3. 数据库结构影响；
4. API 变化；
5. 模块影响；
6. 重要设计决策；
7. 未解决问题；
8. 验证方式与结果；
9. 下一步建议。

如果某项不适用，明确写“无”，不得用省略代替。涉及线上事实时，区分“已部署”“真实验证”“本地验证”和“规划/未完成”，不得把规划当作实现结果。

### 12.4 共享文档职责

共享知识按需维护，代码、数据库和云端事实优先于过时的说明文字；实现变化涉及对应领域时同步更新文档：

- `docs/architecture.md`：系统边界、模块关系、运行环境和跨模块依赖；当前不存在时，只有出现稳定架构决策才新增。
- `docs/database.md`：数据库集合/表、字段、索引、归属和迁移边界；当前用户端详细设计见 `docs/sql.md`，新增文档应避免重复维护同一字段事实。
- `docs/api.md`：用户端接口、请求/响应、错误码、权限和版本边界；接口变化必须同步更新。
- `docs/business-rules.md`：场景、费用、订单状态和不可破坏的业务规则；产品规则仍以 `prd.md` 为唯一依据。
- `docs/admin.md`：商家后台的账号、权限、状态推进和审计设计；商家端未开始前只记录已确认边界，不虚构接口或线上资源。
- `docs/agent-handoff/`：跨线程交接记录，按任务或模块保存上下文、证据、阻塞和下一步；不得把临时聊天内容当作长期事实。
- `docs/MCP-DEPLOYMENT-RUNBOOK.md`、`docs/WECHAT-POSITIVE-REGRESSION-2026-08-18.md`：分别记录 CloudBase 部署/回滚边界和真实微信正向回归证据，不作为业务规划文档替代品。
- `docs/archive/`：保存不再作为当前实施依据、但必须保留的历史或阶段性文档；归档内容不得直接当作当前业务、架构、接口或数据库事实。

新增或调整共享文档时采用最小范围编辑；没有稳定事实或明确职责时，不为填充目录而创建空文档。历史快照和回归数据不因文档整理被改写。
