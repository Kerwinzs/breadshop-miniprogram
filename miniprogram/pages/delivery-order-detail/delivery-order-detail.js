const store = require('../../utils/store'), remote = require('../../utils/order-repository')
const { formatFen } = require('../../utils/format')
const STATUS = { placed: '已下单', preparing: '制作中', delivering: '配送中', awaiting_shipment: '待发货', in_transit: '运输中', completed: '已完成', canceled: '已取消' }
function isDev() { return typeof __wxConfig !== 'undefined' && __wxConfig.envVersion !== 'release' }
function normalize(raw) { if (!raw) return null; const transportFen = raw.deliveryFeeFen || raw.postageFen || 0; const order = Object.assign({}, raw, { id: raw.orderNo || raw.id, orderStatus: STATUS[raw.orderStatus] || raw.orderStatus, sceneLabel: raw.deliveryMethod === 'shipping' ? '快递邮寄' : '同城外卖', address: raw.addressSnapshot || raw.address, total: formatFen(raw.totalFen), insulation: formatFen(raw.insulationFeeFen), transport: formatFen(transportFen), items: (raw.items || []).map((item) => Object.assign({}, item, { name: item.productName || item.name, lineTotal: formatFen(item.lineTotalFen) })) }); return order }
Page({ data: { order: null, canCancel: false, canReorder: false, canAdvance: false, isDev: false }, onLoad(q) { this.id = q.id }, onShow() { const local = normalize(store.getOrder(this.id)); this.setData({ order: local, canCancel: !!local && local.orderStatus === '已下单', canReorder: !!local && local.orderStatus === '已完成', canAdvance: false, isDev: isDev() }); remote.get(this.id).then((result) => { const order = normalize(result.order); store.updateOrder(this.id, result.order); this.setData({ order, canCancel: order.orderStatus === '已下单', canReorder: order.orderStatus === '已完成' }) }).catch(() => {}) }, cancel() { wx.showModal({ title: '取消订单', content: '确认取消该订单？不涉及退款或资金操作。', success: (r) => { if (r.confirm) remote.cancel(this.id).then(() => { this.onShow(); wx.showToast({ title: '订单已取消' }) }).catch((error) => wx.showToast({ title: error.message || '取消失败', icon: 'none' })) } }) }, reorder() { const result = store.reorder(this.id); if (result.unavailable.length) wx.showToast({ title: `有 ${result.unavailable.length} 件商品未加入`, icon: 'none' }); if (result.added) wx.navigateTo({ url: '/pages/delivery-cart/delivery-cart' }) }, back() { wx.navigateBack() } })
