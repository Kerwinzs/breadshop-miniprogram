const store = require('../../utils/store'), cart = require('../../utils/cart-service'), feeService = require('../../utils/fee-service')
Page({ data: { items: [], method: 'local', address: null, subtotal: '¥0.00', insulation: '¥2.00', transport: '', transportLabel: '配送费', total: '', status: 'missingAddress', message: '', unavailableItems: [], unavailableMessage: '', submitting: false }, onShow() { this.sync() }, sync() { const state = store.getState(), items = store.getCart('delivery'), address = store.getSelectedAddress(), fee = feeService.calculateDeliveryFees({ deliveryMethod: state.deliveryMethod, address, items }), sub = cart.subtotal(items), unavailableItems = store.getUnavailableCartItems('delivery'); this.setData({ items: items.map((item) => Object.assign({}, item, { unitPrice: cart.money(item.unitPriceFen), lineTotal: cart.money(item.unitPriceFen * item.quantity), unavailable: unavailableItems.some((current) => current.id === item.id) })), method: state.deliveryMethod, address, subtotal: cart.money(sub), insulation: cart.money(fee.insulationFeeFen), transport: fee.transportFeeFen === null ? fee.message : cart.money(fee.transportFeeFen), transportLabel: fee.transportLabel, total: fee.status === 'ready' ? cart.money(sub + fee.insulationFeeFen + fee.transportFeeFen) : fee.message, status: fee.status, message: fee.message, unavailableItems, unavailableMessage: unavailableItems.length ? '有商品已售罄或暂不支持当前配送方式，请返回购物车调整。' : '' }) }, method(e) { store.patchState({ deliveryMethod: e.currentTarget.dataset.method }); this.sync() }, submit() { if (this.data.submitting) return; if (!this.data.items.length) return wx.showToast({ title: '购物车为空', icon: 'none' }); if (this.data.unavailableItems.length) return wx.showToast({ title: this.data.unavailableMessage, icon: 'none' }); if (this.data.status !== 'ready') return wx.showToast({ title: this.data.message, icon: 'none' }); wx.showModal({ title: '确认提交订单？', content: '本次仅生成订单，不涉及支付。', success: (result) => { if (!result.confirm) return; this.setData({ submitting: true }); try { const order = store.createDeliveryOrder(); wx.redirectTo({ url: `/pages/order-success/order-success?id=${order.id}` }) } catch (error) { this.setData({ submitting: false }); wx.showToast({ title: error.message || '下单失败', icon: 'none' }) } } }) }, addressPage() { wx.navigateTo({ url: '/pages/addresses/addresses' }) } })
