const mock = require('../../utils/mock'), cart = require('../../utils/cart-service'), store = require('../../utils/store')
Page({ data: { product: null, price: '', specs: [], selectedSpec: null, quantity: 1, cartCount: 0 }, onLoad(q) { const product = mock.products.find((item) => item.id === q.id) || mock.products[0]; this.setData({ product, price: cart.money(product.deliveryPriceFen), specs: product.specs || [] }) }, onShow() { this.setData({ cartCount: cart.count(store.getCart('delivery')) }) }, selectSpec(e) { const spec = this.data.specs[e.currentTarget.dataset.index]; this.setData({ selectedSpec: spec, price: cart.money(this.data.product.deliveryPriceFen + spec.extraFeeFen) }) }, increase() { this.setData({ quantity: this.data.quantity + 1 }) }, decrease() { this.setData({ quantity: Math.max(1, this.data.quantity - 1) }) }, add() { const product = this.data.product, spec = this.data.selectedSpec || this.data.specs[0]; if (product.soldOut) return wx.showToast({ title: '该商品已售罄', icon: 'none' }); const item = { id: `${product.id}:${spec.id}`, productId: product.id, specId: spec.id, specName: spec.name, name: product.name, unitPriceFen: product.deliveryPriceFen + spec.extraFeeFen, artClass: product.artClass }; cart.addItem('delivery', item, this.data.quantity); this.setData({ cartCount: cart.count(store.getCart('delivery')) }); wx.showToast({ title: '已加入配送购物车' }) }, cart() { wx.navigateTo({ url: '/pages/delivery-cart/delivery-cart' }) } })
