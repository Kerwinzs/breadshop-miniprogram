# 面包坊小程序 PRD（简易商城 · 无支付版）

## 1. 文档概述

### 1.1 背景与目标
我们计划推出一个极简面包售卖小程序，提供“到店自取”和“外卖/邮寄”两种购买场景。其中，配送场景支持用户选择“同城外卖”或“快递邮寄”。用户可快速按场景浏览商品、加购并直接下单，无需任何支付环节。产品聚焦于选购和订单跟踪，让交易流程极致轻便。

### 1.2 用户角色
- **普通顾客**：选择到店自取、同城外卖或快递邮寄，浏览商品、加购、直接下单、查看订单。

---

## 2. 功能架构概览
小程序底部导航分为三个 Tab：

- **首页**：品牌展示、自取/外卖·邮寄入口、活动区域。
- **订单**：用户全部订单，支持按场景和状态筛选。
- **个人**：订单入口、配送地址管理、自取门店管理。

购物车作为悬浮组件存在，由首页、商品列表及商品详情页均可进入。

---

## 3. 全局说明

### 3.1 购买场景强制区分
- 从首页选择“到店自取”或“外卖/邮寄”后，整个浏览、加购、下单过程均锁定在该场景下。
- 进入“外卖/邮寄”场景后，用户需进一步选择“同城外卖”或“快递邮寄”；配送方式可在结算前切换，订单提交后不可修改。
- “到店自取”和“外卖/邮寄”两种场景的购物车和订单完全独立，互不影响；同城外卖与快递邮寄共用配送购物车。
- 场景及配送方式信息全程展示，直到订单完成。

### 3.2 订单状态通用定义
| 场景/配送方式 | 状态流转                                             |
| ------------- | ---------------------------------------------------- |
| 到店自取      | 已下单 → 制作中 → 待自取 → 已完成 / 已取消           |
| 同城外卖      | 已下单 → 制作中 → 配送中 → 已完成 / 已取消           |
| 快递邮寄      | 已下单 → 制作中 → 待发货 → 运输中 → 已完成 / 已取消 |

- 仅“已下单”状态下用户可主动取消，取消后订单关闭，无任何资金操作。
- 其余状态由商家端模拟推进，用户仅可查看。

### 3.3 配送费用与展示规则
- “外卖/邮寄”场景的费用由 **商品总额 + 保温包装费 + 配送费/邮费** 构成；到店自取不收取保温包装费、配送费或邮费。
- 选择“同城外卖”时显示“保温包装费”和“配送费”；选择“快递邮寄”时显示“保温包装费”和“邮费”。
- 保温包装需作为独立费用项展示，并显示包装说明；不收费时显示“¥0.00”，不隐藏该费用项。
- 配送费或邮费根据配送方式和收货地址计算；地址未填写或费用尚未计算时显示“待填写地址后计算”，不得误显示为“¥0.00”。
- 商品列表、购物车、确认订单页及订单详情页使用一致的费用名称与金额；所有金额保留两位小数。

---

## 4. 前台页面功能描述

### 4.1 首页
**定位**：快速分流用户到自取或外卖/邮寄场景，展示推荐商品和活动。

**页面元素及功能**：
1. **顶部信息条**  
   - 显示当前默认门店（自取）或收货地址（外卖/邮寄），未设置时引导用户选择。  
   - 点击可切换门店或修改地址。

2. **Banner 轮播区**  
   - 活动、新品图片，点击跳转对应商品列表或活动页。

3. **双入口核心操作区**  
   - **「到店自取」按钮**：文案“到店自取·免配送费”，点击进入自取商品列表，携带所选门店。
   - **「外卖/邮寄」按钮**：主文案“外卖/邮寄”，副文案“同城外卖·快递邮寄”；点击进入配送商品列表，携带收货地址，并默认沿用用户上次选择的配送方式；无历史选择时默认展示配送方式选择。

4. **推荐商品 / 热销模块**  
   - 展示 4~6 款商品缩略信息（图、名、价格）。  
   - 因未选定场景，点击商品时弹出“请选择到店自取或外卖/邮寄”的轻窗，用户选择后进入对应场景的商品详情页，避免场景混乱。  
   - 若用户近期已有明确场景行为，可默认沿用，但仍允许修改。

5. **活动公告栏**  
   - 展示“满赠”“新品尝鲜”等文字提示。

### 4.2 商品列表页（自取 / 外卖·邮寄布局一致）
**顶部**：
- 场景标题（“到店自取”/“外卖/邮寄”），右侧提供切换标签，一键切换到另一场景列表（切换后购物车各自独立）。
- 自取场景显示“取货门店：XXX”，可点击更换；外卖/邮寄场景显示“送至：XXX”，可点击修改地址。
- 外卖/邮寄场景在标题下方提供“同城外卖 / 快递邮寄”切换项，并展示费用提示卡：
  - 同城外卖：显示“保温包装费 ¥XX.XX”“配送费 ¥XX.XX”。
  - 快递邮寄：显示“保温包装费 ¥XX.XX”“邮费 ¥XX.XX”。
  - 地址未填写时，配送费/邮费显示“待填写地址后计算”，点击费用提示卡可进入地址选择。

**分类筛选**：
- 横滑标签：吐司、欧包、甜点、饮品等，选中高亮，商品区随之筛选。

**商品卡片**：
- 商品图、名称、简短描述。
- 价格：按当前场景显示（自取价/配送价），若有划线原价一并展示。
- 加购操作：每张卡片有“+”“-”数量步进器，可快速将商品加入当前场景购物车，实时显示已加数量。
- 点击卡片主体进入商品详情。

**底部悬浮购物车条**：
- 显示当前场景购物车商品总件数和合计金额。
- 点击展开购物车半屏弹窗或进入独立购物车页面。

**外卖/邮寄特有提示**：
- 同城外卖不设起送价；商品金额未达到任何门槛时仍可进入结算。
- 快递邮寄不设包邮门槛；邮费按收货地址与配送方式计算。
- 悬浮条金额默认为商品小计，并标注“包装费、配送费/邮费以结算页为准”，避免与订单合计混淆。

### 4.3 商品详情页
- 商品图片轮播。
- 商品名、销量、描述。
- 规格选择（若有，如尺寸、口味），选定规格影响价格，必须选择后才可加购。
- 价格展示：突出当前场景价格，可同时显示另一场景价格作为参考；配送场景同时标注“另计保温包装费及配送费/邮费”。
- 加购区：数量步进器及“加入购物车”按钮，操作后提示已加入，底部购物车图标实时更新数字。
- 悬浮购物车入口，可随时查看。

### 4.4 购物车
**入口**：底部悬浮购物车图标（所有场景页可见）。

**页面结构**：
- 按场景分页签：“自取购物车”和“配送购物车”，可切换。
- 配送购物车顶部提供“同城外卖 / 快递邮寄”切换，并显示当前收货地址；切换后商品不变，费用实时重新计算。
- 商品列表：每个商品显示图片、名称、规格、单价、数量步进器（可增减）、小计，左滑可删除。
- 支持全选/取消全选。
- 底部汇总：
  - 商品总额。
  - 配送场景：独立显示保温包装费；同城外卖显示配送费，快递邮寄显示邮费。
  - 合计金额（商品总额 + 保温包装费 + 配送费/邮费）。
  - 自取场景：保温包装费、配送费及邮费均为 0，不单独展示费用项。
- **“去结算”按钮**：点击进入确认订单页。配送费用尚未计算时，引导用户先完善地址；其余情况常亮可点。

### 4.5 确认订单页
**路径**：购物车点击“去结算”。

**页面内容**：
1. **场景标识**：“到店自取订单”“同城外卖订单”或“快递邮寄订单”。
2. **取货/收货信息**：
   - 自取：门店名称、地址、营业时间，可切换。
   - 外卖/邮寄：收货地址、联系人、电话，可修改，支持新增地址；同时展示当前配送方式并允许在下单前切换。
3. **商品明细**：商品、规格、数量、单价，只读展示，可返回购物车修改。
4. **费用明细**：
   - 商品总额。
   - 保温包装费（配送场景必须展示，并附简短包装说明）。
   - 配送费（仅同城外卖）或邮费（仅快递邮寄）。
   - 合计金额。
5. **下单操作**：
   - 直接显示“确认下单”按钮，无需任何支付或余额校验。
   - 点击后弹出二次确认：“确认提交订单？”。
   - 确认后订单即刻生成，状态为“已下单”，同时清空对应场景购物车中的已购商品。
6. **下单成功反馈**：
   - 弹窗提示“下单成功”，显示订单号。
   - 提供“查看订单”按钮，跳转至该订单详情页；亦可返回首页。

### 4.6 订单 Tab
**定位**：独立底部导航项，集中管理所有订单。

**页面功能**：
1. **订单列表**：
   - 顶部筛选项：“全部”、“到店自取”、“外卖/邮寄”；进入“外卖/邮寄”后可继续筛选“同城外卖”“快递邮寄”。
   - 各场景下状态快捷筛选栏：已下单、制作中、待自取/配送中/待发货/运输中、已完成、已取消；仅展示当前筛选场景适用的状态。
   - 订单卡片展示：
     - 场景标签（自取/同城外卖/快递邮寄）。
     - 订单状态及简要说明（如“制作中·预计15分钟”）。
     - 商品缩略图、件数、订单总额。
     - 取货/收货简略信息、下单时间。
   - 点击卡片进入订单详情。

2. **订单详情页**：
   - 状态进度条：按订单类型展示对应流转节点（自取：待自取；同城外卖：配送中；快递邮寄：待发货、运输中）。
   - 订单号、下单时间。
   - 商品明细、数量、单价。
   - 费用构成（商品总额、保温包装费、配送费/邮费、合计）。
   - 完整取货/收货信息。
   - 可操作按钮：
     - “已下单”状态下可点击“取消订单”，确认后订单变为“已取消”，无任何金额变动。
     - “已完成”状态下可点击“再来一单”，将相同商品加入对应场景购物车。
     - 其他状态下仅查看，无操作按钮。

### 4.7 个人中心 Tab
**页面组成**：
1. **头部信息**：头像、昵称（可设置）。
2. **我的订单快捷入口**：点击跳转至订单 Tab。
3. **功能菜单**：
   - 我的订单（同跳转）。
   - 配送地址管理（外卖/邮寄地址的增删改查，设默认）。
   - 我的门店（管理常用/收藏的自取门店，方便快速切换）。

### 4.8 地址与门店管理（辅助页面）
- **配送地址管理**：从个人中心进入，支持新建、编辑、删除外卖/邮寄收货地址（联系人、电话、省市区、详细地址、邮政编码），可设默认地址。
- **自取门店选择**：首页定位、自取流程中的门店切换，展示附近门店列表（名称、地址、距离、营业状态），选中即同步至当前自取场景。

---

## 5. 订单状态流转补充说明
- 用户下单后订单直接进入 **“已下单”** 状态，无任何支付或扣款动作。
- 后续由系统（模拟商家）按订单类型自动推进状态：自取订单进入“待自取”，同城外卖订单进入“配送中”，快递邮寄订单依次进入“待发货”“运输中”，最终进入“已完成”。
- 仅 **“已下单”** 状态可由用户取消，取消后状态变为 **“已取消”**，不涉及任何资金变化。
- 已完成订单可查看并操作“再来一单”。

---

## 6. 异常与边界说明
- **配送不设起送价**：同城外卖与快递邮寄均不以商品金额限制结算；仅在地址缺失、超出配送范围或费用无法计算时阻止下单。
- **未选择门店/地址**：首页点击自取或外卖/邮寄入口时若未设门店或地址，弹出引导选择框或定位授权提示。
- **邮费无法计算**：地址缺失、超出可配送范围或计费失败时，邮费显示“暂无法计算”，结算按钮置灰，并引导用户修改地址或重试。
- **配送方式切换**：在配送购物车或确认订单页切换“同城外卖 / 快递邮寄”后，重新校验商品可配送性、保温包装费及配送费/邮费；不可配送商品需明确标记并阻止下单。
- **商品售罄**：商品卡片标记“已售罄”，不可加购；已加入购物车的售罄品在进入结算时提示并自动清理。
- **取消订单**：仅已下单状态可取消，取消后订单立即关闭，无后续动作。
- **金额显示**：商品总额、保温包装费、配送费/邮费及合计金额全部保留两位小数，暂不涉及复杂优惠计算。
