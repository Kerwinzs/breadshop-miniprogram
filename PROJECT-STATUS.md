# 面包坊小程序项目状态

> 本文件记录已核验的当前状态，不以历史规划替代实际环境证据。业务规则以 `prd.md` 为准，后端设计以 `BACKEND-DESIGN.md` 为准。

## 2026-09-05 支付配置健康检查

- 用户已通过 CloudBase 控制台向 `payment`、`payment-notify`、`payment-worker` 保存微信支付服务端环境配置；真实值未进入聊天、Git、项目文档或普通日志。
- `payment` 已通过 CloudBase MCP 仅更新代码，线上保持 `Active`；新增 `configurationStatus` 只返回布尔值，不返回任何配置内容。
- CloudBase MCP 已真实调用该健康检查并确认 `configured=true`，说明 `payment` 所需商户配置的存在性、长度、密钥格式、证书序列号格式及 HTTPS 回调地址结构通过检查。
- 2026-09-07 用户已在控制台保存支付总开关；CloudBase MCP 只读调用 `order.configurationStatus` 权威确认 `paymentEnabled=true`。尚未产生真实支付或退款；商家退款配置、worker 运行证据和小程序体验版资金回归仍待完成。
- 本地完整测试 51/51 通过，商家后台生产构建及合同 smoke 通过；Vite 仅保留既有大包体警告。
- 微信开发者工具已由用户上传小程序 `1.0.1`，备注“支付测试”；工具代码质量中的 AppSecret 检查显示通过。2026-09-07 用户确认已成功设置体验版并取得二维码；尚未完成真机资金回归。
- 2026-09-06 尝试通过 CloudBase MCP 设置 `order.PAYMENT_ENABLED=true` 时，因详情查询会返回完整环境变量值且工具不支持服务端单键合并，按安全规则停止；线上支付开关仍关闭，未产生写请求。
- `order` 已增加不访问数据库的 `configurationStatus`；2026-09-07 复核返回 `paymentEnabled=true`，无需读取环境变量。
- worker 定时器创建回执成功，但 MCP 日志查询不支持仅返回脱敏执行摘要，因此尚不能在不接触完整日志/事件内容的前提下证明最近一次 Timer 实际执行；这属于验证能力限制，不代表执行失败。
- `merchant-auth`、`merchant-admin` 已仅更新函数代码并保持 `Active`；用户保存配置后，2026-09-07 布尔健康检查确认 `authConfigured=true`、`paymentPermissionsReady=true`、`refundQueueEnabled=true`。商家需重新登录以取得包含新权限的短令牌；尚未进行真实退款联调。
- 本地完整测试现为 52/52 通过；本次商家健康检查部署未修改数据库、静态站点、小程序、函数环境变量或真实业务数据。

## 2026-09-03 支付第二阶段（仅本地，未部署）

- 已实现微信支付 API v3 请求签名、小程序支付签名、响应/通知验签、AES-256-GCM 解密，以及 JSAPI 下单、查单、关单、整单退款的可注入 Provider。
- 已实现本地受限 HTTPS 客户端和 `http.main` 回调适配器候选；尚未部署，CloudBase 网关原始 body 保真仍待专项验证。
- 已实现签名保护的支付 worker：主动查单、15 分钟超时关单、退款执行、有限批次和失败重试；渠道状态未知时不释放库存，关单后迟到支付成功优先。
- 已完成存量无支付订单兼容：显示为 `not_required`，不允许补发支付，但可继续历史履约流程；新订单明确 `paymentRequired=true`。
- 本轮未配置真实商户参数、未连接真实 HTTP、未创建线上集合/索引/触发器、未部署、未产生资金操作。
- 本地 45 项测试、JavaScript 语法检查、`git diff --check` 和商家端生产构建通过；Vite 保留既有大包体警告。
- 2026-09-05 CloudBase MCP 只读复核：环境正常；线上仅有 7 个既有函数，尚无 `payment`/`payment-worker`；9 个既有集合中尚无三个支付集合；商家静态应用当前为 `breadshop-backend-023`、`SUCCESS`。

## 2026-09-05 支付基础设施部署

- 已在 `cloud1-d9gc800bmc6952073` 创建空的 `paymentTransactions`、`paymentEvents`、`refunds`，均为 `PRIVATE`，并分别建立 `outTradeNo`、`eventId`、`refundNo` 唯一索引。
- `payment(index.main)`、`payment-worker(worker.main)`、`payment-notify(http.main)` 均以 Event 函数部署并核验为 `Active/Available`；worker 禁止小程序/公网直接 invoke，暂未启用定时器。
- 已创建匿名公网回调 `/wechatpay/payment` 与 `/wechatpay/refund`，均指向 `payment-notify` 并开启完整路径透传；无签名负向请求返回 `FAIL`，支付集合仍为 0 条。
- 原生 HTTP 函数实验资源 `payment-callback` 因缺少 `scf_bootstrap` 处于 `CreateFailed`，未删除，也未用于路由。
- 尚未注入真实商户配置，未部署支付版 `order`、`merchant-admin`、商家静态站点或小程序，未产生真实资金操作。

## 当前状态

- 项目阶段：用户端 CloudBase MVP、商家后台和支付基础设施均已部署；支付配置健康检查通过且测试环境总开关已开启，但尚未完成真实资金回归，不能视为正式收款可用。
- 开发环境：`cloud1-d9gc800bmc6952073`，绑定小程序的 CloudBase 传统 NoSQL 云数据库；不使用 SQL/RDB 适配层。
- 云函数：`auth`、`catalog`、`address`、`fee`、`order` 已通过 MCP 部署并核对为 `Active/Available`。
- 数据集合：`users`、`products`、`stores`、`addresses`、`orders`、`orderStatusHistory`、`feeQuotes` 已创建；`feeQuotes` 已设置为 `PRIVATE`。
- 索引：`users(openid)`、`feeQuotes(ownerOpenId, quoteId)` 唯一索引已通过 MCP 创建；`orders(ownerOpenId, clientRequestId)` 已有唯一索引。
- 线上函数版本已包含本地 T-008 的事务写入、配送报价绑定/一次性消费和安全 DTO 改动。
- MCP 未登录调用已得到 `AUTH_REQUIRED`，验证了云函数不接受缺失的微信身份上下文；真实微信身份正向回归记录见 `docs/WECHAT-POSITIVE-REGRESSION-2026-08-18.md`。
- 发布隔离已核对：生产 `miniprogram/` 不注册、不引用开发者回归页；工具源码仅保留在 `tests/tools/dev-regression/`，用户导航和订单详情无调试入口。
- 本地工作区当前有未提交的云函数、页面与测试改动；本状态文档不代表这些改动已经提交、推送或发布。

## 2026-09-03 支付模块本地开发

- 已确认首期规则：三场景全额预付商品小计、运输到付、15分钟超时、仅支付成功可制作、制作前可取消、已支付整单退款、不支持部分退款。
- 本地新增 `payment` 云函数领域层、支付/退款状态投影、小程序支付交互和商家支付门禁；缺真实商户配置时明确拒绝，不模拟成功。
- 本地计划使用 PRIVATE `paymentTransactions`、`paymentEvents`、`refunds`，但集合、权限和唯一索引均未在线创建。
- 尚未实现或验证：CloudBase HTTP 原始通知适配、完整 API v3 签名验签、主动查单、超时定时任务、退款 worker、存量订单兼容迁移和隔离环境真实回归。
- 本阶段未部署、未配置真实密钥、未修改云端数据、未产生资金操作。详细边界见 `docs/payment.md`。

## 2026-08-19 商家后台权限与浏览器回归

- 共享云函数安全规则已受控更新：原 `*` 条目保持为拒绝匿名身份，仅新增 `merchant-auth` 的 `auth != null` 调用条目；MCP 读取已核验规则结构，`auth`、`order`、`address` 与 `merchant-admin` 均继续匹配原默认规则。
- 真实独立后台应用的匿名 CloudBase 身份已成功调用 `merchant-auth`：正确商家登录、刷新后的会话校验、错误密码拒绝和登出均已验证。
- 商家订单列表对 `merchant-admin` 的请求仍返回 `PERMISSION_DENIED`，未执行订单、商品、门店或审计写入。
- 本轮未从浏览器直接调用用户端 `auth`、`order`、`address`，上述三项仅完成规则结构核验，不能标记为真实匿名调用回归。

## 已完成能力

- 用户端微信登录、商品与门店远程读取；登录或网络不可用时，商品、门店浏览和本地购物车仍可降级。
- 云端地址的查询、新增、编辑、删除与默认地址维护。
- 自取、同城外卖和快递邮寄报价、下单、订单列表、订单详情与取消；两个购买场景的购物车保持独立。
- 服务端按微信 OPENID 校验地址、商品、规格、门店与费用；客户端提交的价格、金额、订单状态和归属均不作为可信数据。
- 配送报价持久化在 `feeQuotes`，绑定用户、地址、配送方式和商品快照；订单事务中校验报价有效性并将其消费。
- `orders`、`orderStatusHistory` 的订单创建/取消写入使用事务，`clientRequestId` 用于幂等；安全 DTO 不暴露内部归属字段或文档 ID。

## 当前业务限制

- 支付和整单退款代码及基础设施已部署，但尚未完成真实资金回归；余额和部分退款不在首期范围。
- 不接真实配送、真实邮费、物流面单或轨迹；云函数中的 200/600/1200 分费用仅用于开发联调。
- 商家后台支付门禁和退款请求代码已部署，商家支付权限与退款队列配置健康检查均已通过；真实商家退款联调尚未执行。完整库存流水和正式财务报表仍未实现。
- 当前 `cloud1` 是开发/测试环境，不能用作生产环境。

## 未完成证据与下一步

1. T-009 五项负向验收（报价篡改、报价过期、报价重复消费、相同 `clientRequestId` 幂等和非 `placed` 状态取消）以及普通到店自取下单 `INTERNAL_ERROR` 修复，均已由用户真实微信人工确认完成；未记录未获提供的错误码、trace 或截图细节。
2. 后端用户端已收口。唯一下一目标是商家后台第一阶段闭环：账号与服务端权限、订单状态推进、商品售罄/恢复销售、门店营业/休息和审计；建议派单范围为“后端/权限设计 → 商家网页实现 → 真实订单状态回归”，不得复用用户端 Mock 推进逻辑。
3. 真实履约规则确认后，再替换开发联调费率并接入配送/邮费服务。

## 临时数据规则

- 基线回归数据为 2 条临时地址和 3 条临时订单；本轮正向回归新增 1 条地址和 2 条配送订单，当前共保留 3 条地址和 5 条订单。
- 这些记录是回归证据，当前不得删除、覆盖或迁移；需要清理时必须先获得明确授权，并记录集合、条件、数量、操作者和时间。

## 负向回归边界

- 报价篡改、过期、重复消费、相同 `clientRequestId` 和非 `placed` 状态取消已由用户人工确认完成；本记录仅如实标注确认来源，不补写未提供的请求或响应细节。
- 不使用内存数据库、伪造身份或客户端本地状态模拟上述结果；如开发者工具或 CloudBase 连接失败，暂停回归并请求人工处理连接问题。
- 以上五项以及普通 pickup `INTERNAL_ERROR` 修复的证据来源均为用户真实微信人工确认；本记录不补写未提供的请求、响应、错误码或 trace 细节。

## 版本与部署记录

- 已有本地提交：`f8c0c60`（云登录和目录读取）、`76f0fcf`（地址与订单云端化）、`ba883f0`（云端订单状态展示）。
- 每次 MCP 部署后，在 `TASKS.md` 记录目标 EnvId、函数名、部署时间、MCP 返回的状态/版本标识、集合权限与索引核验结果；同时在本文件写明回归证据和未完成项。
- 标准操作步骤见 `docs/MCP-DEPLOYMENT-RUNBOOK.md`。历史临时数据不属于部署清理范围。

## 2026-08-18 部署与正向回归记录

- EnvId：`cloud1-d9gc800bmc6952073`；函数 `auth/address/fee/order` 的 MCP 状态均为 `Active/Available`，部署时间分别为 09:34:22、09:38:28、09:38:36、09:38:43。
- 真实微信身份正向记录：登录、地址新增/编辑/默认切换、到店自取、同城外卖、快递邮寄、订单列表/详情和自取取消，详见 `docs/WECHAT-POSITIVE-REGRESSION-2026-08-18.md`。
- T-009 五项负向验收和普通 pickup `INTERNAL_ERROR` 修复均已由用户真实微信人工确认完成；后端用户端收口，下一阶段为商家后台第一阶段闭环。

## 2026-08-24 商家商品图片发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；发布时工作树为 dirty，部署内容来自当前未提交工作树，不代表 Git 提交或生产基线。
- `merchant-admin`：通过 CloudBase MCP 仅更新函数代码，保留既有运行时和服务端环境变量；发布后状态为 `Active`，运行时 `Nodejs16.13`，云端修改时间为 `2026-08-24 09:50:15`。无商家令牌调用 `uploadProductImage` 返回 `AUTH_REQUIRED`。
- 静态应用：复用 `breadshop-backend`，当前版本为 `breadshop-backend-009`，状态 `SUCCESS`，构建时间 `2026-08-24 09:54:16`；固定域名及 `/products` 深链接已验证加载新版资源。
- 真实商家会话回归：使用未提交的隔离商品表单上传竖版 JPEG 样本，线上处理结果为 `1200×1600`，保持原始约 `3:4` 比例；后台预览、移除引用均成功。未提交测试商品，因此未新增商品或商品审计记录。
- 存储清理：仅删除本轮精确测试文件 `product-images/codex_img_reg_20260824/1787536736730-2e3d4fc97682980c0253.jpg`；删除后该精确前缀文件数为 0。未删除目录、历史商品图片、集合或审计日志。
- 未完成证据：尚未在微信真机验证新 `cloud://` 商品图的列表 `aspectFill` 与详情 `aspectFit` 展示；小程序代码也未在本轮部署。商家端新增/保存包含 `imageUrls` 的真实商品、替换后保存、版本冲突和保存失败仍需隔离测试商品回归。

## 2026-08-24 商品分类与描述发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；发布内容来自当前 dirty 工作树，不代表 Git 提交或生产基线。
- 数据库：新建 `productCategories` 集合并设为 `PRIVATE`，创建 `categoryId_1_unique` 唯一索引；按线上现有商品分类初始化 5 条启用记录：吐司、欧包、甜点、饮品、其他。未改写现有 `products`、订单或历史商品快照。
- 云函数：通过 CloudBase MCP 更新 `catalog` 与 `merchant-admin` 代码；最终均为 `Active/Available`，云端修改时间分别为 `2026-08-24 14:30:53`、`2026-08-24 14:30:56`。线上代码已核验包含动态分类读取和分类创建、编辑、删除处理。
- 静态应用：复用 `breadshop-backend`，发布版本 `breadshop-backend-010`（BuildId `2601908817`），状态 `SUCCESS`，构建时间 `2026-08-24 14:32:12`。线上入口引用 `index-Dwee5WoJ.js`，其 SHA-256 与本地构建产物一致，并包含商品分类管理、商城卡片简述和商品详情介绍界面。
- 安全负向回归：无微信身份调用 `catalog.listProducts`、无商家会话调用 `merchant-admin.listProductCategories` 均返回 `AUTH_REQUIRED`；未绕过现有身份边界。
- 未完成证据：本轮未使用真实商家会话执行分类新增、改名、停用或删除，也未修改真实商品描述；小程序端动态分类及详情描述尚未进行真实微信身份/真机正向回归。

## 2026-08-24 商品规格编辑器发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；发布内容来自当前 dirty 工作树，不代表 Git 提交或生产基线。本轮未迁移数据库，未修改真实商品、订单或历史商品快照。
- 云函数：通过 CloudBase MCP 更新 `merchant-admin` 与 `catalog`；最终均为 `Active/Available`，云端修改时间分别为 `2026-08-24 14:47:31`、`2026-08-24 14:47:34`。线上代码已核验包含至少一个启用规格校验、停用规格过滤及无可选规格保护。
- 静态应用：复用 `breadshop-backend`，发布版本 `breadshop-backend-011`（BuildId `2601912364`），状态 `SUCCESS`，构建时间 `2026-08-24 14:49:33`。线上 JS/CSS 与本地构建产物 SHA-256 一致，并包含规格新增、名称/加价编辑、启停、上下排序、删除确认和表单校验界面。
- 安全负向回归：无商家会话调用 `merchant-admin.saveProduct`、无微信身份调用 `catalog.listProducts` 均返回 `AUTH_REQUIRED`；未绕过现有身份边界，也未产生商品写入。
- 未完成证据：尚未使用真实商家账号保存规格新增、调价、停用、恢复、排序或删除，也未在微信真机验证停用规格不展示及规格顺序。

## 2026-08-24 订单打印与导出发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；发布内容来自当前 dirty 工作树，不代表 Git 提交或生产基线。本轮未修改数据库结构、订单数据、权限或历史订单快照。
- 云函数：通过 CloudBase MCP 更新 `merchant-admin`，最终为 `Active/Available`，云端修改时间 `2026-08-24 15:02:15`。线上代码已核验包含订单分批读取与既有 `orders.address.read` 地址权限控制。
- 静态应用：复用 `breadshop-backend`，发布版本 `breadshop-backend-012`（BuildId `2601891992`），状态 `SUCCESS`，构建时间 `2026-08-24 15:03:21`。线上 JS/CSS 与本地构建产物 SHA-256 一致，包含全部订单打印、当前筛选 CSV 导出、配送收件信息、自取门店信息和专用打印样式。
- 安全负向回归：无商家会话调用 `merchant-admin.listOrders` 返回 `AUTH_REQUIRED`；未读取或导出真实订单和客户信息，未触发真实打印。
- 未完成证据：尚未使用真实商家会话检查自取、同城外卖、快递订单的打印预览，也未以超过一页的真实筛选结果核对 CSV 条数、中文编码和敏感信息列。

## 2026-08-24 商家后台易用性与视觉改造发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；发布内容来自当前 dirty 工作树，不代表 Git 提交或生产基线。本轮仅发布静态应用，未更新云函数、数据库、权限或业务数据。
- 静态应用：复用 `breadshop-backend`，发布版本 `breadshop-backend-013`（BuildId `2601912687`），状态 `SUCCESS`，构建时间 `2026-08-24 15:15:17`。
- 线上资源：JS/CSS 与本地构建产物 SHA-256 一致；已核验新版经营概览、订单工作台、商品与分类、门店管理、操作记录导航及天蓝、奶油白、草莓粉品牌色进入生产资源。响应式、打印和既有功能样式包含在同一已校验 CSS 构建中。
- 功能边界：本轮仅调整商家后台信息架构、视觉层级和响应式布局，订单打印导出、分类、规格、图片、门店、审计和权限逻辑保持不变。
- 未完成证据：尚未使用真实商家登录态对桌面宽屏与手机窄屏进行人工视觉走查，也未在真实浏览器打印预览中检查新版布局。

## 2026-08-24 商家端品牌改名发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；本轮仅发布静态应用，未更新云函数、数据库、权限或业务数据。
- 对外展示名称统一为“UDii 有笛-商家端”；工程与 CloudBase 服务标识继续使用 `breadshop-backend`，避免影响现有域名和部署链路。
- 静态版本：`breadshop-backend-014`（BuildId `2601914057`），状态 `SUCCESS`，构建时间 `2026-08-24 15:25:16`。
- 线上核验：浏览器 `<title>` 为“UDii 有笛-商家端”，登录页与登录后顶栏品牌文本已进入生产 JS；线上 JS 与本地构建产物 SHA-256 一致。

## 2026-08-24 订单下单时间筛选发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；本轮更新 `merchant-admin` 与商家后台静态应用，未修改数据库结构、订单数据或权限。
- 云函数：`merchant-admin` 最终为 `Active/Available`，云端修改时间 `2026-08-24 15:37:40`；线上代码已核验包含 `orderStartDate`、`orderEndDate` 和非法日期范围拒绝逻辑，按中国时区自然日筛选。
- 静态应用：版本 `breadshop-backend-015`（BuildId `2601912880`），状态 `SUCCESS`，构建时间 `2026-08-24 15:38:57`。
- 线上核验：生产 JS 包含下单开始/结束日期选择、稳定日期参数及“导出当前筛选”联动；线上 JS 与本地构建产物 SHA-256 一致。
- 安全负向回归：无商家会话调用带日期参数的 `merchant-admin.listOrders` 返回 `AUTH_REQUIRED`；未读取或修改真实订单。
- 未完成证据：尚未使用真实商家登录态验证单日、跨日、跨月和空结果筛选，也未核对带日期条件的真实 CSV 导出条数。

## 2026-08-24 商品双场景展示预览发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；本轮仅更新商家后台静态应用，未更新云函数、数据库、权限或真实商品数据。
- 静态应用：版本 `breadshop-backend-016`（BuildId `2601913251`），状态 `SUCCESS`，构建时间 `2026-08-24 16:16:39`。
- 线上核验：生产 JS 已包含到店自取/外卖邮寄双场景商品卡片与详情预览、首图封面说明和图片上移/下移排序；生产 CSS 包含预览卡片与双栏布局。线上 JS/CSS 与本地构建产物 SHA-256 一致。
- 数据边界：继续复用 `imageUrls`、`desc`、`detailDesc`、`priceFen`、`deliveryPriceFen`、`specs`，未新增字段；本轮未调用商品保存接口或修改真实商品。
- 未完成证据：尚未使用真实商家登录态打开商品编辑弹窗进行人工视觉走查，也未保存真实图片排序验证用户端实际展示。

## 2026-08-24 商品分场景上架与小程序上传记录

- 发布环境：`cloud1-d9gc800bmc6952073`；发布内容来自当前 dirty 工作树，不代表 Git 提交或生产基线。未迁移数据库，未修改真实商品、订单、权限或历史订单商品快照。
- 云函数：`catalog` 已更新并真实核验为 `Active/Available`，云端修改时间 `2026-08-24 19:35:25`；线上代码包含按到店自取、同城外卖和快递邮寄能力筛选商品，同时保留无场景参数时的兼容行为。
- 商家静态应用：版本 `breadshop-backend-017`（BuildId `2601979083`），状态 `SUCCESS`，构建时间 `2026-08-24 19:36:24`；线上入口引用 `index-DPZaksC-.js` 与 `index-BTG-WgD8.css`，JS SHA-256 与本地构建产物一致，并包含“到店自取上架”和“外卖/邮寄上架”独立控制。
- 微信小程序：经用户明确授权开启微信开发者工具 CLI 服务端口；CLI 登录态为已登录。版本 `1.0.20260824` 已上传成功，描述为“商品支持到店自取、外卖/邮寄分场景上架”，上传包总计 `128198` bytes。
- 发布边界：上述小程序结果仅代表代码已上传至微信开发者平台，不代表已提交微信审核、审核通过或正式发布；后续审核与发布状态需在微信公众平台确认。

## 2026-08-24 首页今日推荐配置发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；发布内容来自当前 dirty 工作树，不代表 Git 提交或生产基线。本轮未修改真实商品、推荐列表、订单、权限、索引或历史订单商品快照。
- 云函数：通过 CloudBase MCP 更新 `merchant-admin` 与 `catalog`，最终均为 `Active/Available`，云端修改时间分别为 `2026-08-24 20:22:25`、`2026-08-24 20:22:31`。线上代码已核验包含 `saveHomeRecommendations`、`listHomeRecommendations`、最多 6 件限制、推荐排序及审计记录。
- 商家静态应用：版本 `breadshop-backend-018`（BuildId `2601980434`），状态 `SUCCESS`，构建时间 `2026-08-24 20:23:28`。线上入口引用 `index-f3xj_4cP.js`；线上与本地 JS SHA-256 均为 `37a725ccac713d8268959d10c59f66eac54a4733e6815703e2624f03908e8382`，并包含“设置今日推荐”和对应保存接口。
- 微信小程序：版本 `1.0.20260824.2` 已通过微信开发者工具 CLI 上传成功，描述为“商家端可配置首页今日推荐商品与顺序”，上传包总计 `129038` bytes。
- 数据模型：推荐设置使用 `products.homeRecommended` 与 `products.homeRecommendOrder` 两个可选字段；无需集合迁移或新增索引。只有商家实际保存推荐列表时才会写入这些字段，本轮部署未执行该操作。
- 发布边界：小程序结果仅代表代码已上传至微信开发者平台，不代表已提交微信审核、审核通过或正式发布；尚未使用真实商家会话保存推荐列表，也未在真机验证保存后的首页顺序。

## 2026-08-24 首页推荐图片修复上传记录

- 问题原因：首页推荐卡片已取得商品 `primaryImageUrl`，但 WXML 仅渲染装饰色块，未创建图片组件。
- 修复内容：首页推荐卡片绑定商品首图，使用 `aspectFill` 和稳定容器展示；无首图时保留原装饰占位，不影响商品点击和购买场景选择。
- 微信小程序：版本 `1.0.20260824.3` 已通过微信开发者工具 CLI 上传成功，描述为“修复首页今日推荐商品图片展示”，上传包总计 `129264` bytes。
- 验证：今日推荐专项测试和首页 JavaScript 语法检查通过。本记录只代表开发版本上传成功，不代表微信审核通过或正式发布；真机图片加载仍需人工确认。

## 2026-09-03 快递必拍真实商品发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；发布内容来自当前 dirty 工作树，不代表 Git 提交或生产基线。用户已明确授权本轮必要的数据补齐与发布；未删除、迁移或改动无关真实数据。
- 数据补齐前只读核验：`productCategories.name=必拍选项` 与三个固定 `productId` 均返回 0 条，因此不存在需要保留的商家自定义内容。随后创建分类 1 条、商品 3 条；插入结果分别为 `insertedCount=1` 和 `insertedCount=3`。
- 分类真实核验：`categoryId=shipping-required`、名称“必拍选项”、`enabled=true`、`sortOrder=999`、`version=1`。
- 商品真实核验：`shipping-required-packaging`、`shipping-required-sf-collect`、`shipping-required-notice` 均归属“必拍选项”，启用且未售罄，`supportsPickup=false`、`supportsLocalDelivery=false`、`supportsShipping=true`，只含启用的 `standard` 规格且加价为 0。泡沫冰袋配送价为 200 分；另外两项自取价和配送价均为 0。名称、图片、简述和详情说明仍可由商家维护。
- 云函数：`catalog`、`fee`、`order`、`merchant-admin` 均通过 CloudBase MCP 更新代码并真实核验为 `Active/Available`、`CodeResult=success`、运行时 `Nodejs16.13`；云端修改时间依次为 `2026-09-03 10:07:10`、`10:07:20`、`10:07:19`、`10:07:18`。
- 商家静态应用：版本 `breadshop-backend-019`（BuildId `2602190137`），状态 `SUCCESS`，构建时间 `2026-09-03 10:08:42`。线上入口引用 `index--cyh2G0v.js`，线上与本地 JS SHA-256 均为 `cfaae23bf877f04866d35b290345358571a90b487c69a40fff7e4e1fe20b38e5`，并包含快递必拍商品管理、稳定标识和名称可维护界面。
- 微信小程序：开发版本 `1.0.20260903` 已通过微信开发者工具 CLI 上传成功，描述为“快递必拍选项改为真实商品并按目录计价”，上传包总计 `134693` bytes。未提交微信审核、未正式发布。
- 费用边界：快递邮寄不另收固定保温包装费、配送费或邮费；泡沫冰袋价格作为真实商品进入小计，默认顺丰特快到付与拍前必读服务端强制零价，同城外卖既有费用逻辑保持不变。
- 未完成证据：尚未以真实微信身份完成快递购物车固定 3 项、不可删除/增减、报价、下单、取消回补、订单详情和再来一单的端到端真机回归；尚未以真实商家会话修改名称、图片、说明或泡沫价格并验证前台同步。

## 2026-09-03 快递必拍列表合并与分类改名发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；发布内容来自当前 dirty 工作树，不代表 Git 提交或生产基线。
- 商家端：删除商品页顶部独立“快递必拍选项”专区；三个固定商品回到普通商品表格并显示“快递必拍”标签，固定分类改名为“拍前必读”。
- 数据更新：`productCategories(categoryId=shipping-required)` 已改为“拍前必读”；三个固定商品的 `category` 已逐条改为“拍前必读”。首次使用 MCP 更新时确认该工具为整条替换语义，随即依据写入前只读快照完整恢复四条记录；最终逐条复查确认稳定标识、名称、图片、说明、价格、规格、上架范围和排序均完整，未影响其他商品或订单。
- 云函数：`catalog`、`fee`、`order`、`merchant-admin` 均已更新，真实核验为 `Active/Available`，修改时间 `2026-09-03 10:30:22`；`fee` 与 `order` 已使用“拍前必读”校验固定商品分类。
- 商家静态应用：版本 `breadshop-backend-020`（BuildId `2602188824`），状态 `SUCCESS`，构建时间 `2026-09-03 10:32:52`。线上入口引用 `assets/index-BeVg3iY5.js`，线上与本地 JS SHA-256 均为 `86a12af4c85a7a8cdae8cefb24be6c3ecea72a94d001cb96886da2603824cfed`。
- 微信小程序：开发版本 `1.0.20260903.1` 上传成功，描述为“快递必拍商品归入拍前必读分类并优化商家端列表”，上传包 `134693` bytes；未提交审核、未正式发布。
- 验证：后台生产构建、商品 UI、快递固定商品、购物车隔离、后台接口和云函数语法测试均通过；线上分类及三商品逐条只读核验通过。
- 未完成证据：尚未使用真实商家会话人工检查商品页视觉，也未用真实微信身份重新完成快递报价与下单回归。

## 2026-09-03 商品排序统一发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；同城外卖与快递邮寄统一使用目录商品排序，不改变同城费用或快递必拍业务规则。
- 排序规则：`sortOrder` 从小到大；相同值时最近保存的商品优先，再以稳定商品标识兜底。快递必拍商品自动加入购物车的顺序同步跟随目录，不再使用固定 ID 顺序。
- 云函数：`catalog` 已通过 CloudBase MCP 更新并真实核验为 `Active/Available`、`CodeResult=success`，云端修改时间 `2026-09-03 10:47:10`；线上代码包含 `compareProductOrder` 和同序处理。
- 微信小程序：开发版本 `1.0.20260903.2` 上传成功，描述为“统一同城外卖与快递商品排序并修复同序展示”，上传包 `135030` bytes；未提交审核、未正式发布。
- 验证：快递必拍排序、购物车隔离、后台契约、目录和小程序语法、`git diff --check` 均通过。尚未以真实微信身份人工核对同城与快递两个商品页的最终顺序。

## 2026-09-03 同城外卖必拍与到付规则发布记录

- 发布环境：`cloud1-d9gc800bmc6952073`；发布内容来自当前 dirty 工作树，不代表 Git 提交或正式生产基线。未修改历史订单或无关真实数据。
- 业务规则：同城外卖与快递邮寄一致采用运输费到付；同城外卖不再另收固定保温包装费或配送费。商家可通过真实商品 `local-required-packaging` 的配送价控制打包费，`local-required-delivery-collect` 与 `local-required-notice` 固定零价。三件商品均为“拍前必读”分类、`standard` 规格、local-only、各固定 1 件。
- 数据真实核验：线上三件同城商品均存在；排序依次为 1、2、3，配送价依次为 200、0、0 分，场景能力均为 `supportsPickup=false`、`supportsLocalDelivery=true`、`supportsShipping=false`。本轮创建时返回的文档 ID 分别为 `d32b92d06a98e67c0107bdf473c93fb3`、`f9ecc4af6a98e69105b070b166264363`、`d32b92d06a98e69b0107bf2450c524c1`。
- 云函数：`catalog`、`fee`、`order`、`merchant-admin` 已通过 CloudBase MCP 更新；最终列表状态均为 `Active`，云端修改时间分别为 `2026-09-03 11:17:15`、`11:17:41`、`11:18:30`、`11:18:44`。报价快照版本为 `delivery-required-products-v4`。
- 分类排序：用户端保留服务端分类顺序；分类按分类排序值、最近更新时间、稳定标识排序，商品按商品排序值、最近更新时间、稳定标识排序；配送必拍商品的自动加入顺序跟随目录结果。
- 历史兼容：新同城订单显示“同城配送费到付”，打包费包含在商品小计；已有非零保温包装费/配送费的历史订单仍按原快照展示和打印，没有迁移或覆写历史记录。
- 商家静态应用：最终版本 `breadshop-backend-022`（BuildId `2602192764`），状态 `SUCCESS`，构建时间 `2026-09-03 15:35:39`。线上入口引用 `index-HKoohIfj.js` 与 `index-B4pGUIko.css`；JS/CSS 与本地产物 SHA-256 分别一致为 `5ca1fbde69bd947a4c8225fba22b28855bbba2df3b08bc3fcc648624703e2c87`、`728702872d7e46452586b6526a0580a3ca129b43e6cb47f1cbf3397866ea7890`。
- 微信小程序：最终开发版本 `1.0.20260903.4` 已通过微信开发者工具 CLI 上传成功，描述为“同城外卖采用打包商品和配送费到付并兼容历史订单”，上传包 `136534` bytes；未提交微信审核、未正式发布。
- 本地验证：云函数和相关小程序 JavaScript 语法检查、配送必拍项、购物车隔离、后台契约、分类、商品列表、订单打印测试、`git diff --check` 与商家端生产构建全部通过。Vite 仅保留既有大包体警告。
- 未完成证据：尚未以真实微信身份完成同城外卖切换、三件必拍商品锁定、报价、下单、订单详情和历史订单兼容的真机端到端回归；尚未以真实商家会话修改同城打包商品价格并验证前台同步。

## 2026-09-03 同城费用提示与必拍库存校验修复发布记录

- 小程序修复：同城外卖商品页移除旧的“保温包装费 ¥2.00”固定提示，地址可配送时改为“配送费到付”；地址缺失、不完整或超出配送范围时仍显示对应错误提示。
- 商家端修复：配送必拍商品打开编辑时自动使用系统库存值 `999999`，不再因线上旧记录缺少 `stockQuantity` 而触发禁用库存框的必填错误；列表显示“固定充足”，普通商品库存规则不变。
- 静态应用：版本 `breadshop-backend-023`（BuildId `2602194085`），状态 `SUCCESS`，构建时间 `2026-09-03 15:56:21`。线上入口引用 `index-Bx3rhmZ0.js`；线上与本地 JS SHA-256 均为 `db68b91bbeedb430fcb5f9c0f2f73c1013b42674c74ce140cd14e17c5beae8ee`，并核验包含“配送必拍”“固定充足”和库存说明。
- 微信小程序：开发版本 `1.0.20260903.5` 上传成功，描述为“修正同城配送费到付提示和必拍商品库存校验”，上传包 `136423` bytes；未提交审核、未正式发布。
- 验证：小程序页面语法、配送必拍商品、后台商品映射测试、`git diff --check` 和商家端生产构建均通过。本轮未更新云函数、数据库、权限、索引或真实商品数据。

## 2026-09-04 商品当前筛选导出发布记录

- 功能：商家后台“商品与分类”页新增“导出当前筛选”，沿用当前关键词和分类条件，自动分页读取并导出全部匹配商品，而不是只导出当前表格页。CSV 包含商品标识、名称、分类、自取价、配送价、库存、销售状态、三种场景上架状态、规格、排序、今日推荐和更新时间。
- 静态应用：版本 `breadshop-backend-024`（BuildId `2602205727`），状态 `SUCCESS`，构建时间 `2026-09-04 14:37:12`。线上入口引用 `index-Imb3Kms0.js`，线上与本地 JS SHA-256 均为 `05a18986a441c66193d0617aa85416de0f5af3383fd9b110d31b6d353a348013`；线上资源已核验包含“导出当前筛选”“库存数量”“同城外卖上架”和“快递邮寄上架”。
- 验证：商品导出专项测试、配送必拍商品、商品列表映射、分类测试、`git diff --check` 和商家端生产构建均通过。本轮未更新云函数、小程序、数据库、权限、索引或真实商品数据。

## 2026-09-12 个人页待自取订单同步修复（本地待上传）

- 个人页“我的订单”快捷卡片现在会在显示本地缓存后主动刷新云端订单，并统一识别服务端状态值（包括 `ready_for_pickup`），因此商家推进到“待自取”后，用户回到个人页可看到最新状态。
- 本轮仅修改小程序前端与测试，未更新云函数、数据库、权限、索引或真实订单；需上传新的体验版后再用真实微信回归。

## 2026-09-12 订单订阅消息授权入口（模板 ID 待配置）

- 方案 A：到店自取、同城外卖、快递邮寄统一申请“取餐提醒”模板；下单确认后请求一次性订阅授权，用户拒绝不影响下单。
- 微信公众平台模板 ID 已配置为 `EW6MUqeJSj81vkRSNJgxBLAk-wAwEBt3b4CXg0GNM9M`；`subscribe-notify` 云函数已在 CloudBase 环境创建并核验为 `Active/Available`。商家订单状态推进触发尚未接线，暂不代表通知已端到端可用。
- 2026-09-12 已配置服务端微信凭证并修正模板真实字段映射（`thing6`、`character_string22`、`phrase16`、`thing23`、`thing20`）；受控调用返回消息 ID，通知接口调用成功。
